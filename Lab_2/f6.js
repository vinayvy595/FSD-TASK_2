const user = { name: 'Afroz', age: 30, city: 'Hyderabad', country: 'INDIA' };

// Using object destructuring to extract name and city
const { name, city } = user;

console.log(name); 
console.log(city);
